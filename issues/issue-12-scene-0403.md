# Issue 12 | Scene 04-3 HTML 实现

## 全局约定

## 全局约定

- 输出路径命名：`Scene 01-1` -> `compositions/scenes/scene-0101.html`，`Scene 11-2` -> `compositions/scenes/scene-1102.html`。
- 每个 HTML 必须是 1920x1080、16:9 横版、浅色科技背景、技术讲解博主风格。
- 每个 HTML 必须使用 `DESIGN.md` 中的 token：`Background`、`Text Primary`、`Text Secondary`、`Tech Blue`、`Governance Purple`、`AI Orange`、`Risk Red`、`Done Green`、`Warning Yellow`。
- 每个 scene 主视觉元素不得超过 6 个；动画只做主视图内状态变化，不做跨画面入场、大幅旋转、复杂粒子。
- 每个 scene 的字幕是观点字幕，不是口播全文；底部字幕必须清晰可读。
- 每个 scene 完成后需可单独打开对应 HTML 预览，并能按合同时间点完成状态变化。

## Issue 内容
- 任务标题：实现 Scene 04-3 独立 HTML
- 时间：01:40 - 01:51
- 目标：用 AI 需求审查弹窗证明源头质量把关，拦截一句话需求。

### 输入

- 合同来源：`scene-contract.md` -> `Scene 04-3`
- 分镜参考：`storyboard.md` -> `Scene 04-3`
- 视觉规范：`DESIGN.md`
- 使用 token：`Background`、`Text Primary`、`Text Secondary`、`AI Orange`、`Risk Red`、`Done Green`
- 画面主体：AI 审查弹窗、评分区、优化建议区、风险提示区、验收标准补全区、质量闸门。
- 必须出现：评分、优化建议、风险提示、验收标准补全；“把一句话需求，拦在评审之前”。
- 动画步骤：
  - 01:40 - 01:44：评分区域放大强调，需求质量从低分状态切换为可评审状态。
  - 01:44 - 01:48：风险提示切换为已识别，验收标准补全区切换为完整。
  - 01:48 - 01:51：评审入口前形成 `AI Orange` 质量闸门，未补全需求显示阻断状态。
- 字幕：源头完成质量把关；把一句话需求，拦在评审之前。
- 禁止事项：禁止弹窗内容过密；禁止只展示评分不展示建议；禁止闸门遮挡核心字幕。

### 输出

- HTML 文件：`compositions/scenes/scene-0403.html`
- 文件要求：独立 scene HTML，可被后续总片或预览器加载；样式和动画写在文件内或项目约定的共享资源中。
- 命名要求：不得改名，不得输出到 `scenes/`、`renders/` 或其他目录。

### 验收标准

- `compositions/scenes/scene-0403.html` 文件存在，打开后显示 Scene 04-3。
- 时间点与动画步骤一致：
  - 01:40 - 01:44：评分区域放大强调，需求质量从低分状态切换为可评审状态。
  - 01:44 - 01:48：风险提示切换为已识别，验收标准补全区切换为完整。
  - 01:48 - 01:51：评审入口前形成 `AI Orange` 质量闸门，未补全需求显示阻断状态。
- scene 主视觉元素不超过 6 个，且包含合同规定的主体：AI 审查弹窗、评分区、优化建议区、风险提示区、验收标准补全区、质量闸门。
- 必须出现内容完整：评分、优化建议、风险提示、验收标准补全；“把一句话需求，拦在评审之前”。
- 字幕准确显示：源头完成质量把关；把一句话需求，拦在评审之前。
- 遵守禁止事项：禁止弹窗内容过密；禁止只展示评分不展示建议；禁止闸门遮挡核心字幕。
- 具体画面验收：弹窗四区可辨；质量闸门表达“评审前拦截”；下一 scene 可由闸门后方转为轨道。
