# Issue 01 | Scene 01-1 HTML 实现

## 全局约定

## 全局约定

- 输出路径命名：`Scene 01-1` -> `compositions/scenes/scene-0101.html`，`Scene 11-2` -> `compositions/scenes/scene-1102.html`。
- 每个 HTML 必须是 1920x1080、16:9 横版、浅色科技背景、技术讲解博主风格。
- 每个 HTML 必须使用 `DESIGN.md` 中的 token：`Background`、`Text Primary`、`Text Secondary`、`Tech Blue`、`Governance Purple`、`AI Orange`、`Risk Red`、`Done Green`、`Warning Yellow`。
- 每个 scene 主视觉元素不得超过 6 个；动画只做主视图内状态变化，不做跨画面入场、大幅旋转、复杂粒子。
- 每个 scene 的字幕是观点字幕，不是口播全文；底部字幕必须清晰可读。
- 每个 scene 完成后需可单独打开对应 HTML 预览，并能按合同时间点完成状态变化。

## Issue 内容
- 任务标题：实现 Scene 01-1 独立 HTML
- 时间：00:00 - 00:06
- 目标：用一个问题建立全片冲突：多系统并行发布为什么会失控。

### 输入

- 合同来源：`scene-contract.md` -> `Scene 01-1`
- 分镜参考：`storyboard.md` -> `Scene 01-1`
- 视觉规范：`DESIGN.md`
- 使用 token：`Background`、`Text Primary`、`Text Secondary`、`Tech Blue`、`Risk Red`
- 画面主体：中心标题区、系统图标组、需求单图标、测试单图标、发布单图标、群聊消息图标。
- 必须出现：标题“多系统发布，为什么会失控？”；浅色科技背景；周边业务信息图标；底部观点字幕。
- 动画步骤：
  - 00:00 - 00:02：背景图标透明度轻微波动，主视图焦点收敛到中心标题区。
  - 00:02 - 00:04：系统、需求、测试、发布、群聊五类图标按类别高亮，复杂度逐步提升。
  - 00:04 - 00:06：周边图标连接线轻微错位，并在画面左侧聚成四条时间线的前置形态。
- 字幕：多系统发布，为什么会失控？
- 禁止事项：禁止黑底粒子风；禁止满屏图标堆叠；禁止标题被背景图标遮挡；禁止大幅旋转和复杂入场动效。

### 输出

- HTML 文件：`compositions/scenes/scene-0101.html`
- 文件要求：独立 scene HTML，可被后续总片或预览器加载；样式和动画写在文件内或项目约定的共享资源中。
- 命名要求：不得改名，不得输出到 `scenes/`、`renders/` 或其他目录。

### 验收标准

- `compositions/scenes/scene-0101.html` 文件存在，打开后显示 Scene 01-1。
- 时间点与动画步骤一致：
  - 00:00 - 00:02：背景图标透明度轻微波动，主视图焦点收敛到中心标题区。
  - 00:02 - 00:04：系统、需求、测试、发布、群聊五类图标按类别高亮，复杂度逐步提升。
  - 00:04 - 00:06：周边图标连接线轻微错位，并在画面左侧聚成四条时间线的前置形态。
- scene 主视觉元素不超过 6 个，且包含合同规定的主体：中心标题区、系统图标组、需求单图标、测试单图标、发布单图标、群聊消息图标。
- 必须出现内容完整：标题“多系统发布，为什么会失控？”；浅色科技背景；周边业务信息图标；底部观点字幕。
- 字幕准确显示：多系统发布，为什么会失控？
- 遵守禁止事项：禁止黑底粒子风；禁止满屏图标堆叠；禁止标题被背景图标遮挡；禁止大幅旋转和复杂入场动效。
- 具体画面验收：标题 1 秒内可读；背景为 `Background` 浅色系；焦点始终在中心问题；结束时能自然承接四条角色时间线。
