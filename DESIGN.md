# HyperFrames Visual Design Gate

任何合成 HTML 编写前都必须先读取本文件。本项目不使用默认色、不使用泛科技黑底粒子风，也不制作传统企业 PPT 汇报片。

## Style Prompt

浅色科技背景，技术讲解博主风格，清爽、专业、有图解感。画面用问题引入、机制拆解、系统截图证明和关键词总结来解释 AI 原生 BizDevOps 版本火车系统。整体避免传统企业宣传片、赛博朋克、复杂粒子和满屏文字；让评委在不听口播时也能理解每章核心观点。

## Canvas

- 主版本：16:9 横版，1920x1080。
- 成片时长：5 分钟以内。
- 主要场景：竞赛评审播放、会议大屏播放、PPT 嵌入、视频平台横屏提交。
- 竖版 9:16 只作为后续裁切方向，不作为当前主设计标准。

## Colors

| Role | Use | Hex |
| --- | --- | --- |
| Background | 米白、浅灰、极浅蓝灰科技底 | `#F8FAFC`, `#F3F4F6`, `#E5E7EB` |
| Text Primary | 标题和核心观点 | `#111827` |
| Text Secondary | 说明文字和弱信息 | `#374151`, `#6B7280` |
| Tech Blue | 系统主线、流程节点、标题强调 | `#2563EB`, `#1677FF` |
| Governance Purple | BizDevOps、治理体系、章节主视觉 | `#6D28D9`, `#7C3AED` |
| AI Orange | AI 赋能点、智能体、自动分析、算法建议 | `#F97316`, `#FA8C16` |
| Risk Red | 风险、失控、冲突、变更告警 | `#EF4444`, `#F5222D` |
| Done Green | 通过、可执行、上车、完成、质量闭环 | `#22C55E`, `#52C41A` |
| Warning Yellow | 预警、提示、待确认 | `#FACC15`, `#FAAD14` |

## Typography

- 字体方向：清晰、现代、技术感强的无衬线。
- 推荐字体：`HarmonyOS Sans`, `Alibaba PuHuiTi`, `Source Han Sans SC`, `PingFang SC`, 系统默认无衬线。
- 禁用字体：书法、卡通、综艺感字体。
- 一级标题：章节开场使用，10 到 18 个字。
- 二级标签：解释关键词，每个标签不超过 6 个字。
- 说明文字：只在必要时出现，单屏不超过 2 行，每行不超过 18 字。
- 主字幕是观点字幕，不是口播全文。

## Layout

- 推荐结构：顶部章节标题，中部关键图解或截图，底部一句结论字幕，角落放关键词标签。
- 每章只讲一个核心机制。
- 图解优先使用手绘箭头、虚线流程、8px 以内圆角矩形、卡片式节点、轻量图标。
- 截图是可信度来源，但同屏最多突出 1 到 2 个重点。
- 内容容器使用 flex 和 padding 布局，避免硬编码绝对定位内容主体。

## Screenshot Source Priority

- 制作分镜、scene contract 或 scene HTML 前，必须先检查 `assets/screenshots/` 下是否有对应 scene 或章节截图。
- 优先使用 `assets/screenshots/processed/` 中已裁切、脱敏、压暗或局部放大的处理稿作为分镜内容填充。
- 如果没有处理稿，再使用 `assets/screenshots/raw/` 中的对应原始截图，并在画面中通过裁切、放大框、高亮框控制信息密度。
- 对应关系优先按 scene 编号匹配，例如 `Scene03.png`、`Scene05-3.png`、`scene-0503`；其次按章节和画面语义匹配。
- 有对应截图时，不要凭空重绘具体产品界面；只允许用图解补充截图无法表达的流程关系。
- 如果没有对应截图，使用抽象图解或截图占位，并在任务/注释中标明素材缺口。

## Screenshot Treatment

- 截图入画前先分析可读区域、可裁切区域、敏感信息和需要高亮的 1 到 2 个重点。
- 已裁切或处理过的截图优先作为主视觉；原始截图只作为备选或裁切来源。
- 截图作为背景时降低透明度或轻微模糊。
- 核心区域用放大框展示。
- 关键字段用 2 到 3px 高亮描边框。
- 蓝框表示普通流程或系统能力。
- 橙框表示 AI 能力。
- 红框表示风险、变更、告警。
- 绿框表示通过、完成、可执行。
- 避免全屏静态截图停留超过 5 秒。

## Motion

- 动效服务理解，不服务炫技。
- 每 3 到 5 秒完成一个明确视觉动作。
- 推荐：高亮框亮起、局部放大、光标指向、标签弹出、信息流动、混乱线条被拉直、群聊文本被结构化、风险红色转为可控绿色。
- 章节开场用 1 秒出现章节编号和章节标题。
- 多场景必须有转场；每个场景都有入口动画，除最后一场外不提前做退出动画。
- 避免字幕飞来飞去、大幅旋转、频繁缩放、大量粒子遮挡主体、每个元素都动导致注意力分散。

## Chapter Formula

每章固定结构：

1. 问题出现
2. 机制拆解
3. 系统截图证明
4. 关键词总结

## What NOT To Do

- 不做整页文字和表格堆叠的传统 PPT 汇报片。
- 不做大量粒子、光效、3D 隧道但看不清系统价值的科技宣传片。
- 不做只有页面点击、没有治理逻辑解释的纯操作录屏。
- 不喊“颠覆、革命、领先”等空泛营销词。
- 不使用大面积纯黑、大面积高饱和紫、花哨粒子背景、无意义 3D 科技空间。
- 不让复杂背景影响截图和字幕识别。
