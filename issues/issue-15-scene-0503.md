# Issue 15 | Scene 05-3 HTML 实现

## 全局约定

## 全局约定

- 输出路径命名：`Scene 01-1` -> `compositions/scenes/scene-0101.html`，`Scene 11-2` -> `compositions/scenes/scene-1102.html`。
- 每个 HTML 必须是 1920x1080、16:9 横版、浅色科技背景、技术讲解博主风格。
- 每个 HTML 必须使用 `DESIGN.md` 中的 token：`Background`、`Text Primary`、`Text Secondary`、`Tech Blue`、`Governance Purple`、`AI Orange`、`Risk Red`、`Done Green`、`Warning Yellow`。
- 每个 scene 主视觉元素不得超过 6 个；动画只做主视图内状态变化，不做跨画面入场、大幅旋转、复杂粒子。
- 每个 scene 的字幕是观点字幕，不是口播全文；底部字幕必须清晰可读。
- 每个 scene 完成后需可单独打开对应 HTML 预览，并能按合同时间点完成状态变化。

## Issue 内容
- 任务标题：实现 Scene 05-3 独立 HTML
- 时间：02:08 - 02:16
- 目标：用班次详情截图证明发布计划有标准、有依据、有留痕。

### 输入

- 合同来源：`scene-contract.md` -> `Scene 05-3`
- 分镜参考：`storyboard.md` -> `Scene 05-3`
- 视觉规范：`DESIGN.md`
- 使用 token：`Background`、`Text Primary`、`Text Secondary`、`Tech Blue`、`Done Green`、`Warning Yellow`
- 画面主体：班次详情截图、容量区域、关键节点区域、快照区域、蓝色高亮框、结论标签组。
- 必须出现：容量、关键节点、版本快照；标准化、可落地、可存档。
- 动画步骤：
  - 02:08 - 02:11：`Tech Blue` 高亮框锁定容量区域。
  - 02:11 - 02:14：高亮框切换到关键节点与快照区域，节点状态转为已确认。
  - 02:14 - 02:16：标准化、可落地、可存档三枚标签依次确认，容量标签转为压力提示。
- 字幕：让多系统发布有标准、有依据、有留痕；标准化｜可落地｜可存档。
- 禁止事项：禁止裸放截图；禁止同屏强调超过两个截图重点；禁止容量压力与完成状态颜色混用。

### 输出

- HTML 文件：`compositions/scenes/scene-0503.html`
- 文件要求：独立 scene HTML，可被后续总片或预览器加载；样式和动画写在文件内或项目约定的共享资源中。
- 命名要求：不得改名，不得输出到 `scenes/`、`renders/` 或其他目录。

### 验收标准

- `compositions/scenes/scene-0503.html` 文件存在，打开后显示 Scene 05-3。
- 时间点与动画步骤一致：
  - 02:08 - 02:11：`Tech Blue` 高亮框锁定容量区域。
  - 02:11 - 02:14：高亮框切换到关键节点与快照区域，节点状态转为已确认。
  - 02:14 - 02:16：标准化、可落地、可存档三枚标签依次确认，容量标签转为压力提示。
- scene 主视觉元素不超过 6 个，且包含合同规定的主体：班次详情截图、容量区域、关键节点区域、快照区域、蓝色高亮框、结论标签组。
- 必须出现内容完整：容量、关键节点、版本快照；标准化、可落地、可存档。
- 字幕准确显示：让多系统发布有标准、有依据、有留痕；标准化｜可落地｜可存档。
- 遵守禁止事项：禁止裸放截图；禁止同屏强调超过两个截图重点；禁止容量压力与完成状态颜色混用。
- 具体画面验收：截图重点明确；三枚结论标签使用 `Done Green`；结束时容量压力能引出纳版冲突。
