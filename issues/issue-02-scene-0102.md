# Issue 02 | Scene 01-2 HTML 实现

## 全局约定

## 全局约定

- 输出路径命名：`Scene 01-1` -> `compositions/scenes/scene-0101.html`，`Scene 11-2` -> `compositions/scenes/scene-1102.html`。
- 每个 HTML 必须是 1920x1080、16:9 横版、浅色科技背景、技术讲解博主风格。
- 每个 HTML 必须使用 `DESIGN.md` 中的 token：`Background`、`Text Primary`、`Text Secondary`、`Tech Blue`、`Governance Purple`、`AI Orange`、`Risk Red`、`Done Green`、`Warning Yellow`。
- 每个 scene 主视觉元素不得超过 6 个；动画只做主视图内状态变化，不做跨画面入场、大幅旋转、复杂粒子。
- 每个 scene 的字幕是观点字幕，不是口播全文；底部字幕必须清晰可读。
- 每个 scene 完成后需可单独打开对应 HTML 预览，并能按合同时间点完成状态变化。

## Issue 内容
- 任务标题：实现 Scene 01-2 独立 HTML
- 时间：00:06 - 00:16
- 目标：说明协同难点不是写代码，而是多角色不在同一条时间线上。

### 输入

- 合同来源：`scene-contract.md` -> `Scene 01-2`
- 分镜参考：`storyboard.md` -> `Scene 01-2`
- 视觉规范：`DESIGN.md`
- 使用 token：`Background`、`Text Primary`、`Text Secondary`、`Tech Blue`、`Risk Red`、`Warning Yellow`
- 画面主体：业务卡片、研发卡片、测试卡片、投产卡片、四条时间线、关键词区。
- 必须出现：业务、研发、测试、投产；“零散沟通 / 人工排期 / 经验判断”；错位时间线。
- 动画步骤：
  - 00:06 - 00:09：四条时间线从平行状态逐渐产生时间偏移。
  - 00:09 - 00:13：关键词逐项高亮，对应时间线出现断点、交叉和错位。
  - 00:13 - 00:16：连接线由 `Tech Blue` 切换到 `Risk Red`，中央断点扩展为混乱现场入口。
- 字幕：业务、研发、测试、投产，不在同一条时间线上；零散沟通｜人工排期｜经验判断。
- 禁止事项：禁止角色卡片超过四类；禁止把口播全文铺满屏；禁止时间线杂乱到不可辨认。

### 输出

- HTML 文件：`compositions/scenes/scene-0102.html`
- 文件要求：独立 scene HTML，可被后续总片或预览器加载；样式和动画写在文件内或项目约定的共享资源中。
- 命名要求：不得改名，不得输出到 `scenes/`、`renders/` 或其他目录。

### 验收标准

- `compositions/scenes/scene-0102.html` 文件存在，打开后显示 Scene 01-2。
- 时间点与动画步骤一致：
  - 00:06 - 00:09：四条时间线从平行状态逐渐产生时间偏移。
  - 00:09 - 00:13：关键词逐项高亮，对应时间线出现断点、交叉和错位。
  - 00:13 - 00:16：连接线由 `Tech Blue` 切换到 `Risk Red`，中央断点扩展为混乱现场入口。
- scene 主视觉元素不超过 6 个，且包含合同规定的主体：业务卡片、研发卡片、测试卡片、投产卡片、四条时间线、关键词区。
- 必须出现内容完整：业务、研发、测试、投产；“零散沟通 / 人工排期 / 经验判断”；错位时间线。
- 字幕准确显示：业务、研发、测试、投产，不在同一条时间线上；零散沟通｜人工排期｜经验判断。
- 遵守禁止事项：禁止角色卡片超过四类；禁止把口播全文铺满屏；禁止时间线杂乱到不可辨认。
- 具体画面验收：四个角色一眼可分辨；时间错位关系清晰；结束时中央红色断点指向下一 scene。
