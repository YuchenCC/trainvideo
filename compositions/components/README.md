# Components

这里保存可复用组件说明或局部片段，例如：

- `callout-frame`：截图高亮框。
- `flow-node`：流程节点。
- `ai-agent-node`：橙色 AI 智能体节点。
- `risk-tag`：红色风险标签。
- `status-tag`：绿色完成标签。

如果组件需要动画，组件说明必须写清：

- 默认尺寸和安全边距。
- 可接受的文本长度。
- 入口动画。
- 禁用的动画属性。
